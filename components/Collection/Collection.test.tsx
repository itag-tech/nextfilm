import { describe, it, Mock, vi } from "vitest";
import { useSearch } from '../../providers/SearchProvider'

describe("Collection", () => {
  it.todo("should render a movie collection when user add a value in the search bar", () => {
  })
});